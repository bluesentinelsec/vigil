import "io";
import "fmt";

fn main() -> i32 {
    i32 n = 1;
    
    while (true) {
        string line, err e = io.read_line();
        if (e != ok) {
            break;
        }
        
        fmt.println(f"     {n}\t{line}");
        n++;
    }
    
    return 0;
}
