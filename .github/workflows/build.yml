name: Build and Release

on:
  push:
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            arch: amd64
          - os: macos-latest
            platform: darwin
            arch: arm64
          - os: windows-latest
            platform: windows
            arch: amd64
    
    runs-on: ${{ matrix.os }}
    
    steps:
      - uses: actions/checkout@v6
      
      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'
      
      - uses: actions/setup-python@v6
        with:
          python-version: '3.x'
      
      - name: Install Python dependencies
        run: pip install pexpect
      
      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential \
            libasound2-dev libx11-dev libxrandr-dev libxi-dev \
            libgl1-mesa-dev libglu1-mesa-dev libxcursor-dev \
            libxinerama-dev libwayland-dev libxkbcommon-dev
      
      - name: Run tests
        run: make test
      
      - name: Build binary
        run: make build
      
      - name: Check binary exists
        shell: bash
        run: |
          echo "Current directory:"
          pwd
          echo "Files in current directory:"
          ls -la
          echo "Looking for basl binary:"
          ls -la basl* || echo "No basl files found"
      
      - name: Get version
        id: version
        shell: bash
        run: |
          # On Windows, Go creates basl.exe even though Makefile says basl
          if [ -f "basl.exe" ]; then
            VERSION=$(./basl.exe --version | cut -d' ' -f2)
          elif [ -f "basl" ]; then
            VERSION=$(./basl --version | cut -d' ' -f2)
          else
            echo "ERROR: basl binary not found!"
            exit 1
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Package binary
        run: |
          if [ "${{ matrix.platform }}" = "windows" ]; then
            7z a basl-${{ matrix.platform }}-${{ matrix.arch }}-${{ steps.version.outputs.VERSION }}.zip basl.exe
          else
            zip basl-${{ matrix.platform }}-${{ matrix.arch }}-${{ steps.version.outputs.VERSION }}.zip basl
          fi
        shell: bash
      
      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: basl-${{ matrix.platform }}-${{ matrix.arch }}-${{ steps.version.outputs.VERSION }}
          path: basl-${{ matrix.platform }}-${{ matrix.arch }}-${{ steps.version.outputs.VERSION }}.zip
      
      - name: Upload to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: basl-${{ matrix.platform }}-${{ matrix.arch }}-${{ steps.version.outputs.VERSION }}.zip
